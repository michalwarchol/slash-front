import { ThemeConfig } from "antd";

const palette = {
  primary: "#7b5ce7",
  secondary: "#8e90f1",
  tertiary: "#bacaf1",
  quaternary: "#a1b7f0",
};

const theme: ThemeConfig = {
  token: {
    colorPrimary: palette.primary,
  },
};

export default theme;
